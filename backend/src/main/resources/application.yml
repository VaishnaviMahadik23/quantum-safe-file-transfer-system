spring:
  application:
    name: quantum-safe-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/quantum_safe_transfer
    username: postgres
    password: 2375
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080